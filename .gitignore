# Environment files (contain user-specific configuration)
.env               # Server configuration (generated by init/configure)
.env.passwords     # Passwords (NEVER commit this - contains plaintext passwords)
!.env.example      # Example config (safe to commit)

# User configuration files (generated by manage.sh)
users.conf         # User definitions (no passwords, but user-specific)
shares.conf        # Share definitions (user-specific)

# Generated docker-compose file
docker-compose.yml

# Docker volumes and configs
config/            # Runtime container configs
shares/            # Actual shared data

# Backups
backups/           # Config backups from ./manage.sh reset
*.tar.gz           # Backup archives

# Backup files from sed operations
*.bak

# Docker Compose overrides
docker-compose.override.yml
