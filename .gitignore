# Environment files
.env               # General config (optional, can be tracked if desired)
.env.passwords     # Passwords only (NEVER commit this)

# Generated configuration (created by manage.sh)
docker-compose.yml

# Docker volumes and configs
config/
shares/

# Backup files from sed operations
*.bak

# Docker Compose overrides
docker-compose.override.yml
